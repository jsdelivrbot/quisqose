var React = require('react');
var Nav = require('Nav');

var PathHop = React.createClass({
  render: function() {
    return (
      <div>
        <Nav />
        <h1>Sup</h1>
      </div>
    );
  }
});

module.exports = PathHop;
