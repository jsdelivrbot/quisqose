var React = require('react');
var Nav = require('Nav');

var Circles = React.createClass({
  render: function () {
    return (
      <div>
        <Nav />
        <h1>sup</h1>
      </div>
    );
  }
});

module.exports = Circles;
